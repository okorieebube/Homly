import React from 'react'

import ListingGridCard from './cards/listing-grid-card'

export default function ListingGrid() {  
    // const cards = props.details;
    // const gridItems = cards.map((card) => 
    //     <ListingGridCard details={card} />
    // );
    
    return (
        <div className="row">
            <div className="col-md-6 mb-6">
                Lorem amas asm sdd jksdj skd sjkd e rusi dus idus dis dus dius dsiduisd uodiao sioas aois aois aois oais as ao sia sa saosiaosaosoa soa
                <ListingGridCard />
            </div>
        </div>
    )
}
